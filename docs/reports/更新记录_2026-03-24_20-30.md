# 更新记录

**日期：** 2026年03月24日 20:30

## 变更摘要

本次更新将 `feat/frontend-ux-optimization` 与 `feat/nlu-kg-quality-first-wave` 的优势按“**UI定稿优先、性能第二、KG质量第三**”策略分阶段合入 `main`。采用 checkpoint 驱动与选择性提交引入，避免了直接 merge 带来的 UI 漂移风险，并已完成远端推送。

## 修改详情

### 分阶段集成结果（main: `8f6cbbc -> 37c4fe8`）

| 阶段 | 关键提交/动作 | 结果 |
|---|---|---|
| RP0 基线 | `rp0-main-8f6cbbc` | 建立回滚锚点 |
| CP1 UI 冻结 | `2b4c678`, `bfdf0da` | 完成 frontend 后续增量引入，保持定稿 UI |
| CP2 门禁基础 | `6986a77`, `420a953`, `48eb44f` + `72b4710`（回退） | 引入 smoke/evaluation/performance 基础；不兼容 UI 条件提交已回退 |
| CP3 KG 核心 | `b9dbe11`, `946391a`, `07dfd0a`, `4d93841` | 完成 KG 策略、实体归一、冲突隔离、增量重要性/摘要缓存 |
| CP4 候选 | `37c4fe8` | 按策略引入数据目录删除提交，形成 cutover 候选 |
| 主干合并与发布 | FF merge 到 `main` 并 `git push origin main` | 远端 `main` 已更新 |

### 关键文件影响

| 文件路径 | 修改类型 | 说明 |
|---|---|---|
| `app.py` | 修改 | 继承 frontend 优化后的侧边栏与交互路径调整 |
| `config.py` | 修改 | 增加 KG 策略开关与回滚配置 |
| `src/engine/game_engine.py` | 修改 | KG 处理链路与运行时行为对齐 |
| `src/knowledge_graph/*.py` | 修改 | 冲突检测、关系抽取、图更新与隔离强化 |
| `src/nlu/entity_extractor.py` | 修改 | 实体别名归一化与置信度输出增强 |
| `src/evaluation/metrics.py` | 修改 | 评估指标能力补齐 |
| `.github/workflows/frontend-smoke.yml` | 新增 | 前端 smoke CI 基础 |
| `tests/evaluation/*` | 新增 | 质量门禁、基准与回归框架 |
| `tests/performance/test_turn_latency.py` | 新增 | 回合延迟性能门禁 |
| `tests/kg/*`, `tests/nlu/*`, `tests/engine/*` | 修改 | 对应能力增强后的回归与一致性校验 |
| `data/*` | 删除 | 按用户确认策略引入数据目录清理 |

## 验证结果

- **阶段性门禁（切换前）**：核心门禁套件 `225 passed`。
- **发布后全量回归（当前）**：`python -m pytest -q` → **276 passed, 0 failed, 0 skipped, 1 warning**。
- **LSP 诊断**：关键改动文件诊断为 **0 errors**。
- **UI 条件测试提交处理**：`3fe63e7` 对当前 UI 冻结结构存在依赖不匹配，已通过 `72b4710` 回退，不影响主线目标。

## 回滚与追踪点

- `rp0-main-8f6cbbc`
- `cp1-ui-lock`
- `cp2-gates-ready`
- `cp3-kg-perf`
- `cp4-cutover-candidate`

## 当前发布状态

- 远端推送：已完成（`main -> origin/main`，目标提交 `37c4fe8`）。
- 结果结论：已实现“UI定稿保持 + 运行流畅性提升 + KG质量修复”的组合目标，可作为当前稳定集成版本。
